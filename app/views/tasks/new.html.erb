<% content_for :title, "Nueva tarea · Taskko" %>

<div class="mb-6">
  <%= link_to "← Volver", last_list_path, class: "text-sm text-ink-muted hover:text-ink transition" %>
  <h1 class="text-2xl font-semibold tracking-tight mt-2">Nueva tarea</h1>
</div>

<div class="rounded-xl border border-line bg-bg-card p-6">
  <%= render "form", task: @task %>
</div>
